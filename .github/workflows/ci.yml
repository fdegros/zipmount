name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  linux:
    # Native Linux VM: /dev/fuse is available, so the full functional test
    # suite (mounting real archives) runs, not just a build check.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libboost-dev libfuse3-dev libicu-dev libzip-dev \
            pkg-config libgtest-dev \
            python3 mount g++-14
      - name: Build and test
        run: |
          make check-fast ASAN=1 UBSAN=1 DEBUG=1
          make clean
          make check-fast ASAN=1 UBSAN=1
          make clean
          make check-fast
          make clean
          # utils/dump-extrafld.cpp uses <print>, which needs GCC >= 14
          # (the rest of the project only requires the documented GCC 13+).
          make utils CXX=g++-14

  macos:
    # Native macOS VM. This only verifies that mount-zip compiles and that
    # its unit tests pass. macFUSE needs a kernel extension that requires a
    # reboot and manual user consent (SIP), which isn't possible on an
    # ephemeral CI runner, so no archive can actually be mounted here:
    # tests/test.py (the functional test suite, run via `make check-fast`)
    # is skipped, but unit_tests (pure GoogleTest, no mount needed) is not.
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies
        run: |
          brew install boost icu4c libzip googletest pkg-config
          # Only needed to get fuse.h and the fuse3.pc pkg-config file for
          # compiling; the bundled kernel extension is never loaded.
          brew install --cask macfuse
      - name: Build
        run: make all
      - name: Unit tests
        run: make unit_tests

  freebsd:
    # GitHub has no native FreeBSD runner. vmactions/freebsd-vm boots a real
    # FreeBSD guest (nested virtualization is only enabled on macOS hosts on
    # GitHub-hosted runners). Unlike macOS, FreeBSD's fusefs kernel module
    # loads without any interactive consent step, so the full functional
    # test suite runs here too.
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build and test in FreeBSD VM
        uses: vmactions/freebsd-vm@v1
        with:
          usesh: true
          # The default rsync-based sync hits a protocol mismatch between
          # macOS's old bundled rsync (the action's host) and FreeBSD's
          # newer one ("unexpected end of file"). sshfs (tried first) was
          # itself unreliable here (mount kept dropping: "remote host has
          # disconnected"), so try nfs instead - the action's other live
          # mount option.
          sync: nfs
          prepare: |
            pkg install -y pkgconf boost-libs icu libzip fusefs-libs3 \
              googletest python3 gmake
          run: |
            kldload fusefs || true
            # The Makefile uses GNU Make syntax (ifeq, $(shell ...)), which
            # FreeBSD's base bmake can't parse.
            gmake check-fast
